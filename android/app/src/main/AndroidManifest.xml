<?xml version="1.0" encoding="utf-8"?>
<!-- Chachi Pharmacy POS for Android — TASK-049. See android/README.md. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The WebView talks to the server on 127.0.0.1, and a LAN receipt printer is raw
         TCP on port 9100 (INT-1). Nothing here reaches the internet (NFR_3.1). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- OPS-001: backups outside the app's own storage. "All files access" on Android 11+;
         the ordinary storage permission before that. A sideloaded POS may ask for both. -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- allowBackup is off: the store's customers and their balances do not go to a cloud
         backup nobody chose (SEC-10). The store's own backups are OPS-001's. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Pos"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode|density"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="sensorLandscape"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
