<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!-- Only for the listing capture (run.sh sets FONTCONFIG_FILE): Roboto, as a phone renders
     system-ui, ahead of whatever this machine would pick. -->
<fontconfig>
  <include ignore_missing="yes">/etc/fonts/fonts.conf</include>
  <dir>FONT_DIR</dir>
  <cachedir>FONT_DIR/cache</cachedir>
  <alias binding="strong"><family>system-ui</family><prefer><family>Roboto</family></prefer></alias>
  <alias binding="strong"><family>sans-serif</family><prefer><family>Roboto</family></prefer></alias>
</fontconfig>
